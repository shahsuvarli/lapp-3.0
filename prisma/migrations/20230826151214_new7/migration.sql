BEGIN TRY

BEGIN TRAN;

-- AlterTable
ALTER TABLE [dbo].[Competitor] ADD [competitor_short_name] VARCHAR(10);

COMMIT TRAN;

END TRY
BEGIN CATCH

IF @@TRANCOUNT > 0
BEGIN
    ROLLBACK TRAN;
END;
THROW

END CATCH
